{
	"attribution": {
		"commit": "",
		"pr": ""
	},
	"permissions": {
		"deny": [
			"Bash(curl:*)",
			"Bash(git push --force*)",
			"Bash(git push -f*)",
			"Bash(git reset --hard*)",
			"Bash(rm -rf *)",
			"Read(./.env)",
			"Read(./.env.*)",
			"Read(./secrets/**)",
			"Write(./.env*)",
			"Write(./secrets/**)",
			"Bash(git branch -D*)",
			"Bash(git clean -fd*)",
			"Bash(git checkout .*)",
			"Read(./**/*.pem)",
			"Read(./**/*.key)",
			"Read(./**/*.cert)"
		]
	},
	"hooks": {
		"Stop": [
			{
				"hooks": [
					{
						"type": "command",
						"command": "\"$CLAUDE_PROJECT_DIR\"/.claude/hooks/remind-version-bump.sh"
					}
				]
			}
		]
	}
}
