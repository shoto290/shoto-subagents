{
  "name": "workflow",
  "version": "0.8.2",
  "description": "Dynamic multi-agent workflows for Claude Code. Ships /onboard — run a project interview to generate a committed project orchestrator, plus an optional per-user operator-profile skill that each user opts into locally via settings.local.json.",
  "author": {
    "name": "shoto290",
    "url": "https://github.com/shoto290"
  }
}
